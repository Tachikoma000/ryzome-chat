import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/contexts/ThemeContext'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Ryzome Agent - AI Assistant for Ryzome',
  description: 'Get instant help with Ryzome features, tutorials, and best practices from our AI assistant.',
  keywords: ['Ryzome', 'AI assistant', 'visual thinking', 'knowledge management', 'canvas'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
